version: '3.3'
services:
  web:
    build: .
    volumes:
      - ./docker/logs:/logs
      - ./docker/pkgs:/pkgs
    ports:
      - "7000:8000"
    user: "${USER_ID}:${GROUP_ID}"
    environment:
      - PYTHONPATH=/app
      - CONDA_PKGS_DIRS=/pkgs