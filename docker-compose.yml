version: '3'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./docker/pkgs:/pkgs
      - ./docker/logs:/logs
    environment:
      - PYTHONPATH=/app
      - CONDA_PKGS_DIRS=/pkgs